const SecondaryButton = ({ text }: { text: String }) => (
  <button className="px-16 py-3 text-xl rounded-lg w-fit text-black border-2 border-primary">
    {text}
  </button>
);
export default SecondaryButton;
